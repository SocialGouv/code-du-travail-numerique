version: '3'

services:

  node:
    container_name: code-du-travail-explorer
    env_file:
      - .env
    build:
      context: .
      dockerfile: ./docker/dev/Dockerfile
    ports:
      - 3000:3000
    restart: always
    volumes:
      - ./conf:/app/conf
      - ./pages:/app/pages
      - ./routes.js:/app/routes.js
      - ./src:/app/src
      - ./static:/app/static
