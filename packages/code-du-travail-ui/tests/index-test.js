import expect from "expect";
import React from "react";
import { render, unmountComponentAtNode } from "react-dom";

describe("Fake test", () => {
  beforeEach(() => {});

  afterEach(() => {});

  it("should work", () => {
    expect(1).toEqual(1);
  });
});
