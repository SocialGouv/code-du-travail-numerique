---
---

import "@cdt/css";
import { Playground, PropsTable } from "docz";
import { Support, Container, Section } from "../src";

## \<Support/\>

<Playground>
  {() => {
    const someVar = "value";
    return (
      <React.Fragment>
        <Section>
          <Support
            onSubmit={event => {
              alert(`submit ${event.target.elements[0].value}`);
            }}
          >
            <input
              type="search"
              className="support__input"
              defaultValue="avignon"
            />
          </Support>
        </Section>
        <Section dark>
          <Support
            onSubmit={event => {
              alert(`submit ${event.target.value}`);
            }}
          />
        </Section>
      </React.Fragment>
    );
  }}
</Playground>

<PropsTable of={Support} />
