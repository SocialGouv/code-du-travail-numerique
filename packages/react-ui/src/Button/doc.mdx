---
name: Button
menu: Component
---

import { Playground, Props } from "docz";
import { X } from "react-feather";
import { Maximize } from "react-feather";
import Section from "../layout/Section"
import { variants } from "../theme";
import { Button } from ".";

# Button

## Standard buttons

<Playground>
  {
    () => ["default"].concat(variants).map((variant) => (
      <Section>
        <Button variant={variant}>{variant} button</Button>
        {" "}
        <Button variant={variant} disabled>{variant} button disabled</Button>
        {" "}
        <Button variant={variant} pressed>{variant} button pressed</Button>
      </Section>
    ))
  }
</Playground>

## Special buttons

<Playground>
  <Section>
    <Button variant="link">Button link</Button>
  </Section>
  <Section>
    <Button variant="icon">
      <X />
    </Button>
  </Section>
  <Section>
    <Button variant="icon">
      <Maximize />
    </Button>
  </Section>
</Playground>

<Props of={Button} />
