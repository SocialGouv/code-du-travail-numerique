---
kind: Service
apiVersion: v1
metadata:
  name: cdtn-frontend${BRANCH_NAME}
  labels:
    app: cdtn-frontend${BRANCH_NAME}
    branch: cdtn${BRANCH_NAME}
spec:
  selector:
    app: cdtn-frontend${BRANCH_NAME}
  ports:
  - port: ${PORT}
    name: frontend
