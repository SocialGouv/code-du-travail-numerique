---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: apm-server${BRANCH_NAME}
  labels:
    app: apm-server${BRANCH_NAME}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: apm-server${BRANCH_NAME}
  template:
    metadata:
      labels:
        app: apm-server${BRANCH_NAME}
    spec:
      containers:
      - name: apm-server${BRANCH_NAME}
        image: docker.elastic.co/apm/apm-server:6.4.2
        resources:
          limits:
            cpu: 1000m
          requests:
            cpu: 100m
        env:
        - name: logging.level
          value: "trace"
        - name: setup.kibana.host
          value: "kibana:5601"
        - name: output.elasticsearch.hosts
          value: "elasticsearch${BRANCH_NAME}:9200"
        - name: output.elasticsearch.enabled
          value: "true"
        ports:
        - containerPort: 8200
