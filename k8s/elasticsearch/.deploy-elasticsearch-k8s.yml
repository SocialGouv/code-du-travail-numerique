.deploy-elasticsearch-k8s:
  image:
    name: $CI_REGISTRY/$IMAGE_INFRA_BASE_NAME/docker-kube:latest
    entrypoint: [""]
  before_script:
  - /apps/create-kubeconfig.sh
  - envsubst < k8s/elasticsearch/statefulset-elasticsearch-dev.yml > k8s/elasticsearch/statefulset-elasticsearch-cdtn-dev.yml
  script:
  - kubectl apply -f k8s/elasticsearch/configmap-elasticsearch-dev.yml
  - kubectl apply -f k8s/elasticsearch/statefulset-elasticsearch-cdtn-dev.yml
  - kubectl apply -f k8s/elasticsearch/service-elasticsearch.yml
  allow_failure: false
